# 🎯 Real Time Object Detection

**A computer vision-based system for detecting and classifying objects from live camera feeds in real time**

---

## 🔧 Installation & Setup

### Clone the Repository

```bash
git clone https://github.com/MaheshwariSahu/Real-Time-Object-Detection.git
cd Real-Time-Object-Detection
```

### Install Dependencies

1. Create a virtual environment (recommended):
    ```bash
    python -m venv venv
    ```

2. Activate the environment:
    - **Windows:**
        ```bash
        venv\Scripts\activate
        ```
    - **macOS/Linux:**
        ```bash
        source venv/bin/activate
        ```

3. Install required dependencies:
    ```bash
    pip install -r requirements.txt
    ```

---

## 🔹 How to Run the Project

To run the file, execute the code:

```bash
python ObjectDetection.py
```

---

## ⌨️ Controls

- Press **Esc** → Exit the application

---